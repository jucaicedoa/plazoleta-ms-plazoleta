# Perfil de test: base de datos en memoria (no requiere PostgreSQL)
spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: false

# Feign: URL dummy para que el contexto cargue (las llamadas se mockean en tests)
feign:
  client:
    config:
      user-service:
        url: http://localhost:9999
